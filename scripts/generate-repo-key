echo "This should only be run if the docker image key is compromised and needs to be regnerated..."
read -p "Are you sure? " -n 1 -r
echo    # (optional) move to a new line
if [[ ! $REPLY =~ ^[Yy]$ ]]
then
    exit 1
fi
ssh-keygen -q -t rsa -N '' -f nginx-layer-6-key